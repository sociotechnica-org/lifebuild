# Sentry CLI authentication
# Create token at: https://sentry.io/settings/account/api/auth-tokens/
# Required scopes: event:read, project:read, org:read (and optionally alerts:read, team:read, member:read)
SENTRY_AUTH_TOKEN=sntryu_your_token_here

# Sentry defaults (optional - can also pass via CLI flags)
SENTRY_ORG=sociotechnica
SENTRY_PROJECT=lifebuild
