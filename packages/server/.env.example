# WebSocket connection to Cloudflare Worker
LIVESTORE_SYNC_URL=ws://localhost:8787

# Store ID for this server instance
STORE_ID=work-squared-server

# Auth token for sync (change in production)
AUTH_TOKEN=insecure-token-change-me

# Port for LiveStore DevTools
DEVTOOLS_PORT=3001

# Node environment
NODE_ENV=development