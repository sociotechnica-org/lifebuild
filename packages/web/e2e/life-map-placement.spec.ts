import { test } from '@playwright/test'

test.describe('Life Map placement tray flow', () => {
  test.fixme(
    'places and removes a project from the map using the unplaced tray (depends on creation flow #709)',
    async () => {}
  )
})
